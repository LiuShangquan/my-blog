backend:
  name: github
  repo: LiuShangquan/my-blog
  branch: main
  # 替换为你的 OAuth 代理地址（必须是 HTTPS，且需实现 GitHub OAuth 交换逻辑）
  # 常见格式示例：https://xxx.netlify.app/.netlify/functions
  # 示例值，必须替换为你自己的 OAuth 代理地址
  base_url: https://your-oauth-proxy.example.com
  auth_endpoint: auth

local_backend: true
# 替换为你的博客正式访问地址（生产环境 URL，建议使用 CDN 加速域名）
# 通常 site_url 与 display_url 一致；仅当展示地址与站点主地址不同才拆分
# 示例值，必须替换为你的实际线上域名
site_url: https://blog.example.com
display_url: https://blog.example.com
logo_url: /favicon.ico

media_folder: source/images/uploads
public_folder: /images/uploads

collections:
  - name: posts
    label: 博客文章
    label_singular: 文章
    folder: source/_posts
    create: true
    delete: true
    extension: md
    format: frontmatter
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: 标题, name: title, widget: string }
      - { label: 发布时间, name: date, widget: datetime, format: "YYYY-MM-DD HH:mm:ss" }
      - { label: 更新时间, name: updated, widget: datetime, format: "YYYY-MM-DD HH:mm:ss", required: false }
      - { label: 标签, name: tags, widget: list, required: false }
      - { label: 分类, name: categories, widget: list, required: false }
      - { label: 是否草稿, name: draft, widget: boolean, required: false, default: false }
      - { label: 正文, name: body, widget: markdown }
